# Local development uses SQLite by default.
# For production, switch prisma/schema.prisma datasource to PostgreSQL and set a PostgreSQL URL.
DATABASE_URL="file:./dev.db"

# Never commit real API keys.
AI_LINKER_ENCRYPTION_KEY="replace-with-local-dev-key"
